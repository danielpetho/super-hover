# Documentation

This file is the **home page**. Edit `content/docs.mdx` to add sections. Heading levels use automatic `id`s so a left **table of contents** can link here later (`h2` / `h3`, etc.).

<Callout variant="info" badge={{ type: "icon", content: "Tip" }}>
  You can use the same MDX components as the rest of the site: `CodeSnippet`, `Callout`, `InstallTabs`, `Video`, `Preview`, `Sandbox`, and more — see `mdx-components.tsx`.
</Callout>

## Getting started

Write prose in Markdown and drop in JSX-style blocks when you need components.

## Inline preview

Built from `embeds/previews/<name>/preview.tsx` after `pnpm generate-sources`. The Code tab appears only when source is available; use `showCode={false}` for demo-only. Optional: `showRestart`.

<Preview name="hello" />

## Math

Display math works when `remark-math` / `rehype-katex` are enabled in `next.config.mjs`:

$$
E = mc^2
$$

Inline: $x^2$.
